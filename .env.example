LENS_AUTH_SECRET_KEY=lens-dev-jwt-signing-secret-2026-default
# Docker Compose defaults to SQLite:
# LENS_DATABASE_URL=sqlite+aiosqlite:////app/data/data.db
# Use an externally managed PostgreSQL database when you need higher concurrency:
# LENS_DATABASE_URL=postgresql+psycopg://lens:password@postgres.example.com:5432/lens
